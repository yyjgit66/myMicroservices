spring:
  application:
    name: api-geteway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: dev
  redis:
    host: 192.168.1.5
    port: 6379
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
      
server:
  port: 8083
  
zuul:
  sensitive-headers: 
  routes:
  #/myProduct/product/productInfo  -> /product/product/productInfo  
    myProduct:
      path: /myProduct/**
      serviceId: product
  #简洁写法
 #   product: /myProduct/**
 #禁止访问的配置
 # ignored-patterns: /**/product/productInfo
management:
  security:
    enabled: false